ACMS_ADMIN_TOKEN=replace-with-a-random-secret
# PostgreSQL is the normal development/deployment database (ADR-0007).
# Plain postgresql:// URLs are auto-normalized to postgresql+asyncpg:// at runtime.
ACMS_DATABASE_URL=postgresql://acms:acms@localhost:5432/acms
ACMS_ENVIRONMENT=development
